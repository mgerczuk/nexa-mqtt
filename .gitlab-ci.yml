image: golang:1.24.0

stages:
 - build

build-job:
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'
  stage: build
  before_script:
    - apt-get update -y -qq
    - apt-get install fakeroot -y
  script:
    - build_package.sh
  artifacts:
    paths:
      - build/*.deb
